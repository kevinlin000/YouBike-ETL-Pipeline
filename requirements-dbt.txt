# Optional analytics dependencies for the dbt scaffold under analytics/dbt.
# dbt-mysql currently lags dbt-core; keep these versions aligned.
# Recommended runtime: Python 3.11.
dbt-core>=1.7,<1.8
dbt-mysql==1.7.0
