version: 2

models:
  - name: stg_station_info
    description: Cleaned station dimension model.
    columns:
      - name: station_no
        description: Unique YouBike station identifier.
        tests:
          - unique
          - not_null
      - name: district
        tests:
          - not_null
      - name: total_spaces
        tests:
          - not_null

  - name: stg_station_status
    description: Cleaned station status fact model with operational risk flags.
    columns:
      - name: id
        tests:
          - unique
          - not_null
      - name: station_no
        tests:
          - not_null
          - relationships:
              to: ref('stg_station_info')
              field: station_no
      - name: record_time
        tests:
          - not_null
      - name: is_stock_out_risk
        tests:
          - accepted_values:
              values: [0, 1]
      - name: is_full_load_risk
        tests:
          - accepted_values:
              values: [0, 1]
